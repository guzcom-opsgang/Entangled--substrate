[package]
name = "entangled-substrate"
version = "0.1.0"
edition = "2021"

[dependencies]
ml-dsa = { version = "0.1.1", features = ["getrandom"] }
sha3 = "0.10"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true

[dev-dependencies]
proptest = "1.5"
